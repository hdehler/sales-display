# Slack Configuration
# Create a Slack App at https://api.slack.com/apps
# Enable Socket Mode and get these tokens:
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
# Channel ID only: Slack → open channel → title → (i) details → bottom “Channel ID” (C… public, G… private).
# Do NOT paste #dev-orders — must look like C01234ABCDEF. Private channel: add Bot scopes groups:read, groups:history + Event message.groups
SLACK_SALES_CHANNEL_ID=

# In api.slack.com → Event Subscriptions, subscribe to BOTH if Slide posts in:
#   message.channels (public)   message.groups (private) — missing groups = no events in private #channels
# Set to 1 to log Block Kit messages that didn’t parse (needs SLACK_SALES_CHANNEL_ID set)
SLACK_DEBUG_PARSE=0
# Log every Socket `message` event (proves whether Slack delivers live events)
SLACK_LOG_MESSAGE_EVENTS=0
# Poll channel history (ms). Default 30000 — picks up other bots (e.g. Slide) if Event Subscriptions lack message.channels. 0 = off
SLACK_POLL_HISTORY_MS=30000

# Import existing channel messages on startup (parsed orders only; no celebrations)
SLACK_BACKFILL_ON_START=false
SLACK_BACKFILL_MAX_MESSAGES=500
SLACK_BACKFILL_PAGE_DELAY_MS=1200

# Slide: quiet period (ms) after the last order before one batched celebration (default 1500)
SLIDE_BATCH_DEBOUNCE_MS=1500
# Full-screen celebration for every Slide order (default on). Set false to only celebrate on CELEBRATION_TRIGGER_PRODUCTS / MILESTONE_INTERVAL
CELEBRATE_SLIDE_ORDERS=true

# Kasa Smart Plug Configuration
# Comma-separated IP addresses of your Kasa plugs (optional if auto-discover is on)
KASA_PLUG_HOSTS=
# Set to "false" to disable auto-discovery
KASA_AUTO_DISCOVER=true

# Celebration Configuration
# How long celebrations last in seconds
CELEBRATION_DURATION=30
# Comma-separated product keywords that trigger celebrations
CELEBRATION_TRIGGER_PRODUCTS=enterprise,premium
# Trigger a milestone celebration every N sales per day (0 to disable)
MILESTONE_INTERVAL=10

# Server
PORT=3000
